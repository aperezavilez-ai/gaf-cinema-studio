# Shared — Schemas & Contracts

Cross-platform types used by engine, mobile shells, and Vercel API.

| Path | Purpose |
|------|---------|
| `schemas/project-state.v1.example.json` | Project State fixture |
| `schemas/deployment-status.v1.json` | `/api/status` response shape |
| `contracts/cloud-api.v1.json` | Cloud API routes (stub until Supabase wired) |

Mobile and web should match these shapes when connecting services.
