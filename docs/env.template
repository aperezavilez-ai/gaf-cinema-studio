# Copy to .env for local Vercel dev (gitignored).
# In production, set these in Vercel → Project → Settings → Environment Variables.

# Public site URL (custom domain)
CINEMASTUDIO_PUBLIC_URL=https://gafcinemastudio.com

# Supabase — pending setup
SUPABASE_URL=
SUPABASE_ANON_KEY=
# Server-only — never expose in iOS/Android bundles
SUPABASE_SERVICE_ROLE_KEY=
